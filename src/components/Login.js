import React, {Component} from 'react';

class Login extends Component {
  state = {

  };

  render() {
    return(
      <form onSubmit={}>
        <label>
          <input type="text"/>
        </label>
        <label>
          <input type="text"/>
        </label>
      </form>
    );
  }
}

export default Login;
